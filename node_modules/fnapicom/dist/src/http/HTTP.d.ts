import { AxiosInstance } from 'axios';
import { RateLimitedAxiosInstance } from 'axios-rate-limit';
import Client from '../client/Client';
import { FortniteAPIResponseData } from './httpStructs';
declare class HTTP {
    client: Client;
    axios: AxiosInstance;
    statsAxios: RateLimitedAxiosInstance;
    constructor(client: Client);
    private fetchWithInstance;
    fetch(url: string, params?: any): Promise<FortniteAPIResponseData>;
    fetchStats(url: string, params?: any): Promise<FortniteAPIResponseData>;
}
export default HTTP;
